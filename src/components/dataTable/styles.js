import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  table: {
    minWidth: 700,
  },
  tableHead: {
    background: "#cc3333",
  },
  tableContainer: {
    borderRadius: 15,
  },
  pagination: {
    height: 50,
  },
});
